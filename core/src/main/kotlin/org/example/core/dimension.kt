package org.example.core

/**
 * Define las dos dimensiones del mundo del juego (A y B).
 * Usar un 'enum' asegura que solo estos dos valores sean posibles (tipado seguro).
 *
 * ---
 * @see "Issue BTG-009: Implementación del Cambio de Dimensión."
 * ---
 */
enum class Dimension {
    A,
    B
}